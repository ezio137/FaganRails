<%= form_for(@financeiro_conta) do |f| %>
  <% if @financeiro_conta.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@financeiro_conta.errors.count, "error") %> impediram esta conta de ser salva:</h2>

      <ul>
      <% @financeiro_conta.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
