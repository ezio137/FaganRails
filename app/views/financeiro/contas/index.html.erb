<div class="bodyTitle">Contas</div>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @contas.each do |conta| %>
  <tr>
    <td><%= link_to 'Exibir', conta %></td>
    <td><%= link_to 'Editar', edit_financeiro_conta_path(conta) %></td>
    <td><%= link_to 'Excluir', conta, confirm: 'Tem certeza?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nova conta', new_financeiro_conta_path %>
